# ZeroTick Public Assets

Public assets for ZeroTick. `docs/` holds documentation screenshots hotlinked by the GitBook docs site (SHA-pinned raw URLs). Treat `docs/` as append-only once pages are live: deleting images breaks published doc revisions.
